## 2024-05-18 - [Add ARIA labels to icon-only buttons]
**Learning:** Found several icon-only buttons (using Material Symbols) without `aria-label`s in `chat.html` and `index.html`. This is a common pattern for icon buttons.
**Action:** Always verify icon-only buttons have descriptive `aria-label`s for screen reader accessibility.
